{
  "name": "komdigi-vibe-code-2026",
  "private": true,
  "scripts": {
    "dev": "slidev --open",
    "build": "slidev build",
    "export": "slidev export --format pdf --with-clicks",
    "preview": "slidev preview"
  },
  "dependencies": {
    "@slidev/cli": "^0.50.0",
    "@slidev/theme-default": "latest",
    "@iconify/json": "^2.2.0"
  }
}
